{
  "scripts": {
    "prebuild": "node ./scripts/generate-engine.js",
    "build": "tsc --build",
    "test": "vitest",
    "bench": "vitest bench"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@vitest/coverage-v8": "^1.6.0",
    "@vitest/ui": "^1.6.0",
    "ts-pegjs": "^4.2.1",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
